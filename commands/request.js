const { requestChannel } = require('../config.json');

module.exports = {
    name: 'request',
    description: 'Request any online staff members to the current channel',
    execute(message, args) {
        
    },
};